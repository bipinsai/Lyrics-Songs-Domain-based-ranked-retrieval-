To create the inverted index run build_index.py with 5000 word.txt as the set of words in the song lyrics as input file and test.csv for songID and word and its frequency pairs as input file.

To perform retreival, run ranking.py and in the console give as input the string which is the subtring of the lyrics of the song you want and it will output the top 10 relevant songs.